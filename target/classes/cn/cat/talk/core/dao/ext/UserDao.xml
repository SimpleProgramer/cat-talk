<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.cat.talk.core.UserDao">
  <!--
    文件的生成时间： 2019-03-19 17:25:34.
    你应该把Mapper类的扩展方法的sql语句放在这个文件里面
  -->
  
  <insert id="batchInsert" parameterType="java.util.List">
    insert into user (user_id, name, gender, phone, account, images, 
      desc, create_time, update_time, delete_status, delete_time) values 
    <foreach item='item' collection='list' separator=','  index=''>
     (#{item.userId,jdbcType=BIGINT}, #{item.name,jdbcType=VARCHAR}, 
      #{item.gender,jdbcType=BIT}, #{item.phone,jdbcType=VARCHAR}, 
      #{item.account,jdbcType=VARCHAR}, #{item.images,jdbcType=VARCHAR}, 
      #{item.desc,jdbcType=VARCHAR}, #{item.createTime,jdbcType=INTEGER}, 
      #{item.updateTime,jdbcType=INTEGER}, #{item.deleteStatus,jdbcType=BIT}, 
      #{item.deleteTime,jdbcType=INTEGER}) 
    </foreach>
  </insert>
</mapper>